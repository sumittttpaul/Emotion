import { NextPage } from 'next';
import { ResetPasswordUI } from '../../components/ui/AuthUI/ResetPasswordUI';

/**
 * @author
 * @function @ResetPassword
 **/

const ResetPassword: NextPage = () => {
  return <ResetPasswordUI />;
};

export default ResetPassword;
