import { SetupUI } from '../../components/ui/AuthUI/SetupUI';
import { NextPage } from 'next';

/**
 * @author
 * @function @Setup
 **/

const Setup: NextPage = () => {
  return <SetupUI />;
};

export default Setup;
