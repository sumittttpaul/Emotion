import { NextPage } from 'next';
import { EmailVerifiedUI } from '../../components/ui/AuthUI/EmailVerifiedUI';

/**
 * @author
 * @function @EmailVerified
 **/

const EmailVerified: NextPage = () => {
  return <EmailVerifiedUI />;
};

export default EmailVerified;
