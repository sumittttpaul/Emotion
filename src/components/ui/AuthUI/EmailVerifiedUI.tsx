import React, { FC } from 'react'

interface IProps {}

/**
* @author
* @function @EmailVerifiedUI
**/

export const EmailVerifiedUI:FC<IProps> = (props) => {
  return (
    <div>EmailVerifiedUI</div>
   )
 }
