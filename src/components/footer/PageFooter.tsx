import React, { FC } from 'react';

interface IProps {}

/**
 * @author
 * @function @PageFooter
 **/

export const PageFooter: FC<IProps> = (props) => {
  return (
    <footer className='w-full bg-[#202020]'>
      
    </footer>
  );
};
